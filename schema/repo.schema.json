{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/AdarGit008/baseline-skill/schema/repo.schema.json",
  "title": "baseline.repo.json — the repo descriptor (C39)",
  "description": "The repo's declared identity and posture: the one stored piece of intent every applicability, severity, and join derivation hangs off. Deliberately minimal and schema-bound — that is what stops it regrowing into a status blob. Keys prefixed with _ are inline notes and are ignored by the validator. Consumed by src/descriptor.mjs; every field's consumer (active or reserved) is asserted by --self-check.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "type", "lifecycle", "maturity", "workflow", "anchoring"],
  "properties": {
    "schema_version": {
      "type": "integer",
      "enum": [1],
      "description": "Descriptor format version. Lets the engine refuse or migrate a descriptor written for a different contract."
    },
    "type": {
      "type": "string",
      "enum": ["node", "python", "service", "library", "docs"],
      "description": "What this repo is. Declared, not sniffed — supersedes filesystem auto-detection so a tooling package.json can't misclassify a docs repo as node."
    },
    "lifecycle": {
      "type": "string",
      "enum": ["experimental", "production", "deprecated"],
      "description": "Lifecycle stage (Backstage catalog-info vocabulary)."
    },
    "maturity": {
      "type": "string",
      "enum": ["prototype", "claimed", "released"],
      "x-strictness": ["prototype", "claimed", "released"],
      "description": "Discrete maturity tier. Gates scrutiny that rises with maturity — e.g. the CLAIM category activates at 'claimed'. x-strictness (ascending) is DESC-03's weakening ladder: a down-move is a posture weakening (M6)."
    },
    "workflow": {
      "type": "string",
      "enum": ["multi-lane", "single-lane", "multi-lane-local"],
      "x-strictness": ["single-lane", "multi-lane-local", "multi-lane"],
      "description": "Agent-workflow posture. multi-lane is the default (a solo dev gets lanes too); single-lane is the readiness-only posture; multi-lane-local (CF5) is the reduced git-plane mode — admit and reconcile work from origin alone and never consult the forge. Under check and orient the forge is closed on every posture (§11 D12): forge-dependent rules resolve n/a, reason 'forge not consulted'. A rule declaring a workflow is evaluated only on a matching posture. x-strictness (ascending) is DESC-03's weakening ladder (M6)."
    },
    "anchoring": {
      "type": "string",
      "enum": ["strict", "relaxed", "off"],
      "x-strictness": ["off", "relaxed", "strict"],
      "description": "Anchoring strictness knob (C15). Orthogonal to workflow — a single-lane repo may still anchor. x-strictness (ascending) is DESC-03's weakening ladder (M6)."
    },
    "ground_truth_boundary": {
      "type": "object",
      "additionalProperties": false,
      "description": "Which ground-truth planes are authoritative and reachable (C29). Consumed by the capability probe and the target-ref policy.",
      "properties": {
        "forge": { "type": "string", "enum": ["github", "none"], "description": "Forge driver. 'none' = offline/at-rest only." },
        "default_branch": { "type": "string", "minLength": 1, "maxLength": 128, "pattern": "^[^\\s~^:?*\\[\\\\]+$", "description": "The target ref enforcing contexts read the descriptor from (FS1). Bounded like the lane globs: the value rides into git argv (origin/<branch>)." }
      }
    },
    "lanes": {
      "type": "object",
      "additionalProperties": false,
      "description": "The lane namespace — the branch family agent work and its session records (records/sessions/<lane>/) are keyed by. Read by no rule under check or orient; its consumers (admit's sister-lane scan, reconcile's lease derivation, log's record path) are the §11 D13 ledger follow-up. namespace is also a DESC-03 structural field (the claim primitive).",
      "properties": {
        "namespace": { "type": "string", "pattern": "^[^*]*\\*[^*]*$", "description": "Branch-name pattern for a lane branch (refs/heads/<namespace>), e.g. lane/*. Exactly one '*', which the issue number replaces — the ref IS the claim (FS2), so the pattern must yield one deterministic name per issue." },
        "lease_ttl": { "type": "string", "pattern": "^[1-9][0-9]*[dh]$", "description": "Lease horizon (nonzero) for reconcile's lease derivation (D13 follow-up); a lane idle past it derives ABANDONED there and is reclaimable — never read under check or orient. Freshness = max(tip committedDate, PR updatedAt) — FS10 as amended." },
        "families": { "type": "array", "maxItems": 64, "items": { "type": "string", "minLength": 1, "maxLength": 64 }, "description": "The repo's OTHER declared branch families beyond the lane namespace (globs, e.g. adopt/*, release/*) — the real branch inventory, consumed by reconcile's lane derivation (D13 follow-up). Bounded (each ≤64 chars; ≤64 entries) since the globs ride into a matcher." }
      }
    },
    "join_keys": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Declared join keys (trailers/frontmatter fields) the relational join is allowed to use. An unresolvable join is a finding, never an inference."
    },
    "engine_pin": {
      "type": "string",
      "description": "Engine version the repo is wired for. Skew-detection input."
    },
    "staleness": {
      "type": "object",
      "additionalProperties": false,
      "description": "Freshness ceilings for derived, bounded-stale views (orient).",
      "properties": {
        "facts_ttl": { "type": "string", "pattern": "^[0-9]+[dhm]$", "description": "How long a cached forge fact is served before re-fetch." },
        "orient_ceiling": { "type": "string", "pattern": "^[0-9]+[dhm]$", "description": "Hard age ceiling past which orient labels its view stale." }
      }
    }
  }
}
