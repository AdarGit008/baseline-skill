{
  "rules": [
    {
      "id": "FLOW-02",
      "title": "A lane branch carries its own session record",
      "category": "flow",
      "severity": "warn",
      "applies_to": "all",
      "workflow": "multi-lane",
      "branch_scope": "lane",
      "source": "https://about.gitlab.com/blog/2018/07/03/solving-gitlabs-changelog-conflict-crisis/",
      "lesson": "C14/C12 — records ride the branch to main (ADR-0009 Rule 3): the forensic tier lands atomically with the change it describes",
      "rationale": "A lane that merges without its session record leaves the next orient blind to why the change happened and what was left open. The record travels in the same PR — the 'update the status' PR class must not exist.",
      "fix": "Write the record on this branch: baseline log -m \"what happened\" --next \"the one next step\". Lands warn; M7 promotes per posture.",
      "sources": [
        "tree",
        "history"
      ],
      "on_unreachable": "skip",
      "contexts": [
        "check",
        "admit"
      ],
      "certainty": "deterministic",
      "check": {
        "kind": "branch-session-record"
      }
    },
    {
      "id": "FLOW-06",
      "title": "A gated subject changes with its record in the same PR",
      "category": "flow",
      "severity": "warn",
      "applies_to": "all",
      "workflow": "multi-lane",
      "branch_scope": "lane",
      "source": "https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions",
      "lesson": "CF9 — C14 same-PR atomicity was enforced for sessions only; the descriptor is V2's largest stored intent (DESC-03's blocker enforcement lands at M6 admit)",
      "rationale": "A descriptor change is a posture change — the judgment saying why belongs in the same range, not a follow-up promise. Heuristic (subject→record mapping is a convention), so warn is its ceiling (C07).",
      "fix": "Add the corresponding record to this branch — for baseline.repo.json changes: baseline jdg new --kind deviation --subject \"descriptor change\" --reason \"...\" --review-by <date>.",
      "sources": [
        "tree",
        "history"
      ],
      "on_unreachable": "skip",
      "contexts": [
        "check",
        "admit"
      ],
      "certainty": "heuristic",
      "check": {
        "kind": "branch-atomicity",
        "pairs": [
          {
            "if_changed": [
              "baseline.repo.json"
            ],
            "expect": [
              "records/judgments/*.json"
            ],
            "note": "baseline.repo.json changed with no judgment record in the same range (DESC-03 preview)"
          }
        ]
      }
    }
  ]
}
