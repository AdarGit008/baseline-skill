{
  "name": "project-baseline",
  "version": "3.0.0",
  "project_types": [
    "node",
    "python",
    "service",
    "library",
    "docs"
  ],
  "description": "Testable readiness standard. The original build/test/context/claims rules were distilled from the author's own repo reviews, then pressure-tested and extended against the prior-art field (OpenSSF Scorecard, SLSA, 12-factor, Google SRE, Diataxis, repolinter, Backstage/Cortex/OpsLevel, and many more). Each rule maps to a lesson or a cited source; blockers fail CI, everything else warns. Rules with a 'pack' only run when that pack is active; everything else is always on. With no config file every pack is off: a pack activates only from an explicit switch — makes_external_claims:true (claims), a non-empty decision_globs (decisions), project_type:service (service) — or from the profiles/packs list (or --profile <pack>) for any pack (see packs).",
  "packs": {
    "claims": "Opt-in via makes_external_claims:true (or profiles/packs/--profile claims). The claims register and per-claim record discipline.",
    "decisions": "Opt-in via a non-empty decision_globs (or profiles/packs/--profile decisions). Decision-record status, edges, amendments and numbering.",
    "descriptor": "Opt-in via profiles/packs/--profile descriptor. The baseline.repo.json descriptor: present, schema-valid, and judgment-carrying when it changes.",
    "service": "Opt-in via project_type=service (or profiles/packs/--profile service). Operability/observability rules that only make sense for a running service.",
    "advanced": "Opt-in via profiles/packs/--profile advanced. Expert/niche rules (SBOM, code-scanning, mutation testing, symbol-integrity) that would be noise on most repos."
  },
  "modules": [
    "rules/build.json",
    "rules/test.json",
    "rules/ctx.json",
    "rules/claim.json",
    "rules/sec.json",
    "rules/gov.json",
    "rules/comm.json",
    "rules/qual.json",
    "rules/repro.json",
    "rules/ops.json",
    "rules/rec.json",
    "rules/desc.json",
    "rules/plug.json"
  ]
}
