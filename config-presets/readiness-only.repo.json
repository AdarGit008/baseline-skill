{
  "_preset": "readiness-only — the minimal posture (baseline.repo.json, NOT a config file): score the repo against the rules and nothing more — anchoring off, no agent-workflow contract. Copy to <repo>/baseline.repo.json, then set type.",
  "schema_version": 1,
  "type": "library",
  "lifecycle": "production",
  "maturity": "released",
  "workflow": "single-lane",
  "anchoring": "off"
}
