{
  "_note": "A schema-invalid baseline.repo.json (maturity 'beta' is not a valid tier). Proves DESC-01 WARN with the deterministic schema error, and that an invalid descriptor's type=docs is NOT honored — project_type falls back to auto-detection (node, from package.json)."
}
