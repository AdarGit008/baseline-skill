# jdg-repo — status

last-verified: {{HEAD1}} {{TODAY}}

Minimal V1-style status stamp so CTX-01 passes; the point of this fixture is the
judgment ledger, not the stamp (which CTX-12 rightly nudges about).
