# Review — Issue #52

Not yet. This branch is the review of the *mcgyvr corpus*, not a code change: it
carries `SPEC.md`, the plan, the todo and four drafted issue bodies, and touches
nothing under `src/`, `rules/`, `check.mjs` or `test/`.

What wants reviewing here is a judgement, not a diff:

1. **The ranking.** Trust damage × frequency, per the interview. Frequency is
   countable and every row names its lanes; trust damage is argued. Disagree with the
   order and the drafts renumber.
2. **The four drafts** (`tasks/issues/F1..F4`). Each reproduces; each stops at
   *suggested fix* rather than picking one. F2's fork in particular is a policy call.
3. **The five unfiled candidates** (`SPEC.md`). The bar was "reproduced against
   baseline-skill code or it stays a candidate". One of the five is refuted outright
   on current main and is recorded as refuted rather than dropped.
4. **The stated limits.** Two of 162 session records were scanned rather than read.

A code review belongs to the lane that takes F1.
