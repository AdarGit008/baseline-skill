{
  "_note": "A consumer repo vendoring tools/baseline/ whose committed lock names an older version (2.4.0) and a stale hash (all-zero — synthetic, unambiguous) while the tree's own rules.json says 2.4.1. Proves REC-06's skew WARN names BOTH versions with the re-pin recipe — the vendor-bump-without-re-pin case the lock exists to catch. Everything else is a bare-minimum repo; REC-06 is the fixture's one subject."
}
