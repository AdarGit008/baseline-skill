{
  "_note": "MERGE — admission-point rules (M6a). MERGE-01 (admission re-derivation) dissolved into the `baseline admit` command itself (re-derivation is what admit IS) and MERGE-03 (post-merge revalidation) dissolved into reconcile's cron (C37) — a rule for either would be presence theater (C22) or a duplicate home (C09); the M6 ruling in docs/v2/PLAN.md §8 is the record. Severity warn until M7's promotion (selfcheck law: admit's refusal is a command contract, not a rule severity).",
  "rules": [
    {
      "id": "MERGE-02",
      "title": "No unmerged sister-lane dependencies (declare stacks with Baseline-Stacked-On)",
      "category": "merge",
      "severity": "warn",
      "applies_to": "all",
      "workflow": ["multi-lane", "multi-lane-local"],
      "branch_scope": "lane",
      "source": "https://graphite.dev/guides/stacked-diffs (stacked-change discipline)",
      "lesson": "C32 — a lane admitted atop another lane's unmerged commits silently depends on work that may never land; an honest stack declares itself, an undeclared one is a latent integration break",
      "rationale": "Deterministic from the git plane alone: a sister lane whose shared history with HEAD reaches past the target tip has unmerged commits inside this admission. The Baseline-Stacked-On trailer (whole-token ref match, anywhere in the admitted range) declares intentional stacking and lifts the finding. Lands warn with promotion metadata; M7 promotes per posture.",
      "fix": "Land the sister lane first and rebase, or declare the stack: commit with trailer 'Baseline-Stacked-On: lane/<N>' in this range.",
      "sources": ["history"],
      "on_unreachable": "skip",
      "contexts": ["admit"],
      "certainty": "deterministic",
      "check": { "kind": "merge-sister-dep" }
    }
  ]
}
